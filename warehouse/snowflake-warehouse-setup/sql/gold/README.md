# Gold SQL Assets

Dataset-level scaffold SQL artifacts for the Gold layer.

Naming convention:

- `gold__<dataset>.sql`

Subfolders:

- `ddl/`: per-dataset table scaffold SQL
- `dml/`: per-dataset transform/load scaffold SQL
