# Silver SQL Assets

Dataset-level scaffold SQL artifacts for the Silver layer.

Naming convention:

- `silver__<dataset>.sql`

Subfolders:

- `ddl/`: per-dataset table scaffold SQL
- `dml/`: per-dataset transform/load scaffold SQL
