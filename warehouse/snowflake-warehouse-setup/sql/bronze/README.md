# Bronze SQL Assets

Dataset-level SQL artifacts for the Bronze layer.

Naming convention:

- `bronze__<dataset>.sql`

Subfolders:

- `ddl/`: per-dataset table creation SQL
- `dml/`: per-dataset ingestion (`COPY INTO`) SQL
